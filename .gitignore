__pycache__/
*.pyc
.env
.env.local
.DS_Store
data/snapshot.json
data/kpis.json
# The public/ copies are generated by `scripts/ingest.py` (Phase 4 will wire
# this in). They are gitignored so we never commit a stale snapshot to GitHub.
public/kpis.json
public/snapshot.json
# wrangler creates a local state dir; safe to ignore.
.wrangler/
# The deploy log lives outside the repo but is a useful artifact.
dashboard_deploy.log
